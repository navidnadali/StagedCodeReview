# RETRY NOTICE

Your previous attempt did not produce a valid findings.json. This is attempt 2
of 2. Follow the Output contract EXACTLY: write ONE valid JSON object matching
the schema to findings.json in your current working directory (raw JSON, no
fences in the file), and repeat the identical JSON as a final ```json fenced
block in your reply. No other prose after the fence. All required keys must be
present; use empty arrays / null where you have nothing.
